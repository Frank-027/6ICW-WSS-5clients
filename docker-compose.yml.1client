version: '3.8'

services:
  # Build the client image from the Dockerfile in the client directory
  client1:
    build: ./client
    image: wss-client
    environment:
      - API_KEY=${API_KEY_CLIENT1}
      - SERVER_URL=${SERVER_URL}
